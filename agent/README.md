## QA Note (Milton - 2026-07-17)
⚠️ This folder is missing the agent's source code (package.json, server files).
Only README.md and a residual package-lock.json exist. Verified via `git log --all -- agent/`.
Reported to Johana for follow-up.# AI agent info 

goes here .....
